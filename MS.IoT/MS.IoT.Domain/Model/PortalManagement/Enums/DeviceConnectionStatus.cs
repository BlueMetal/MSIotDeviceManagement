namespace MS.IoT.Domain.Model
{
    /// <summary>
    /// Enumeration to keep state of a device connection state
    /// </summary>
    public enum DeviceConnectionStatus
    { 
        Connected = 0,
        Disconnected = 1,
        NotActivated = 2
    }
}
