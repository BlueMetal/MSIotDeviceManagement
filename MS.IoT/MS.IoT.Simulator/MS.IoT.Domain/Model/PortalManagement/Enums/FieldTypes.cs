namespace MS.IoT.Domain.Model
{
    /// <summary>
    /// Enumerator that lists all available field types to return and use for filtering
    /// </summary>
    public enum FieldTypes
    {
        String = 0,
        Integer = 1,
        Double = 2,
        Date = 3,
        Boolean = 4
    }
}
